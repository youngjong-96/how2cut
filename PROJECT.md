# 프로젝트 개요
창문을 만들기 위해서는 만들려는 창문 길이만큼의 알루미늄이 필요하다.
일정한 길이의 알루미늄 원자재를 필요한 알루미늄 길이들로 자르는데 최적의 개수와 방법을 매번 생각하는게 시간이 오래 걸린다. (이 때, 최적의 방법은 남는 알루미늄의 길이가 가장 짧은 경우이다.)
알루미늄 원자재 길이, 잘라야 하는 알루미늄 길이와 개수 세트가 주어질 때, 최적의 원자재 개수와 자르는 방법을 한 눈에 보여줄 수 있는 웹 서비스를 개발하고자 한다.

## 목표

- 사용자가 원자재 길이와 절단해야 할 길이/수량을 입력하면 필요한 원자재 개수와 절단 배치를 계산한다.
- 각 원자재별 절단 목록, 남는 길이, 같은 절단 패턴의 반복 횟수를 한눈에 확인할 수 있게 한다.
- 계산 결과를 현장에서 바로 사용할 수 있도록 인쇄, 복사, 공유 가능한 형태로 제공한다.
- 추후 톱날 두께, 절단 여유분, 재사용 가능한 잔재 기준 같은 실제 작업 조건을 옵션으로 확장할 수 있게 설계한다.
- 사용자가 입력값을 입력하는 것과 결과를 확인하기 쉬운 UI, UX를 고려한다.

## 문제 정의

- 입력값
  - 원자재 길이: 예) 6000mm
  - 절단 항목 목록: 예) 1200mm 3개, 800mm 4개
  - 선택 옵션: 절단 손실값, 최소 재사용 잔재 길이, 최대 계산 시간, 최적화 우선순위
- 출력값
  - 필요한 원자재 최소 개수
  - 원자재별 절단 순서와 남는 길이
  - 동일한 절단 패턴 묶음
  - 작업자가 실제로 따라갈 수 있는 길이별 절단 순서
  - 총 필요 길이, 총 원자재 길이, 총 잔여 길이, 사용률
- 최적화 기준
  - 기본값은 사용자가 선택한 우선순위에 따라 달라진다.
  - 최소 잔재 우선: 필요한 원자재 개수와 총 잔여 길이를 가장 먼저 줄인다.
  - 최소 길이 변경 우선: 같은 길이를 최대한 연속해서 자르도록 작업 순서를 만든다.
  - 최소 원자재 변경 우선: 같은 원자재를 가능한 오래 유지하면서 작업하도록 순서를 만든다.
  - 균형 우선: 잔재, 길이 변경, 원자재 변경을 가중치로 함께 평가한다.

# 기술 스택

- 프레임워크: Next.js App Router
- 언어: TypeScript
- UI: React, Tailwind CSS
- 폼 검증: React Hook Form, Zod
- 상태 관리: 초기 버전은 React 상태와 URL 쿼리/localStorage 조합, 복잡해지면 Zustand 도입
- 계산 엔진: 순수 TypeScript 모듈
- 백그라운드 계산: Web Worker
- 테스트: Vitest, Testing Library, Playwright
- 배포: Vercel
- 패키지 매니저: pnpm

## 선택 이유

- Next.js는 Vercel 배포와 궁합이 가장 좋고, 별도 서버 없이 정적/클라이언트 중심 앱으로 빠르게 배포할 수 있다.
- TypeScript는 길이, 수량, 원자재, 절단 패턴 같은 도메인 모델을 명확하게 표현하기 좋다.
- 알고리즘은 서버 비용 없이 브라우저에서 실행하되, 계산량이 커질 수 있으므로 Web Worker로 UI 멈춤을 방지한다.
- Zod를 사용하면 음수 길이, 원자재보다 긴 절단 길이, 빈 입력 같은 오류를 사용자에게 즉시 안내할 수 있다.
- Vitest로 계산 엔진을 촘촘히 검증하고, Playwright로 실제 입력부터 결과 표시까지 확인한다.

## 화면 구성

- 입력 패널
  - 원자재 길이
  - 절단 손실값
  - 절단 항목 행 추가/삭제
  - 길이와 수량 입력
- 결과 요약
  - 필요한 원자재 개수
  - 총 잔여 길이
  - 사용률
  - 계산 방식과 계산 시간
- 절단 배치
  - 원자재별 막대 시각화
  - 절단 조각 라벨
  - 남는 길이 표시
- 패턴 요약
  - 같은 절단 패턴을 묶어서 "이 방식으로 N개 절단" 형태로 표시
- 보조 기능
  - 예시 데이터 불러오기
  - 결과 복사
  - 인쇄용 보기
  - 입력값 URL 공유

# 최적의 방법을 구하기 위한 알고리즘

## 알고리즘 방향

이 문제는 절단 재고 문제(Cutting Stock Problem)이자 1차원 빈 패킹(Bin Packing) 문제에 가깝다. 일반적으로 NP-hard 문제이므로 모든 입력에서 즉시 완전탐색을 하는 방식은 적합하지 않다.

MVP에서는 다음의 하이브리드 방식을 사용한다.

1. 빠른 휴리스틱으로 좋은 상한을 먼저 구한다.
2. 가능한 최소 원자재 개수부터 정확 탐색을 시도한다.
3. 시간 제한 안에 최적해를 찾으면 최적 결과로 표시한다.
4. 입력이 매우 커서 시간 제한을 넘으면 휴리스틱 기반의 가장 좋은 결과와 "근사 결과" 상태를 표시한다.

## 계산 절차

1. 입력 정규화
   - 길이와 수량을 숫자로 변환한다.
   - 길이가 0 이하인 항목, 수량이 0 이하인 항목을 제거하거나 오류 처리한다.
   - 원자재보다 긴 절단 항목은 계산 불가로 표시한다.
   - 절단 손실값이 있다면 각 조각 또는 절단 횟수 기준으로 반영한다.

2. 조각 목록 생성
   - `{ length: 1200, quantity: 3 }`을 `[1200, 1200, 1200]`처럼 확장한다.
   - 긴 조각부터 배치하기 위해 내림차순 정렬한다.

3. 하한 계산
   - 전체 필요 길이 합계를 원자재 길이로 나누어 최소 원자재 개수 하한을 구한다.
   - `lowerBound = ceil(totalRequiredLength / stockLength)`

4. 상한 계산
   - First Fit Decreasing 또는 Best Fit Decreasing으로 빠른 초기 해를 구한다.
   - 이 결과의 원자재 개수를 상한으로 둔다.

5. 정확 탐색
   - 하한 개수부터 상한 개수까지 차례대로 "이 개수의 원자재로 전부 배치 가능한가?"를 검사한다.
   - 가장 먼저 성공하는 원자재 개수가 총 잔여 길이를 최소화하는 최적 개수다.
   - 탐색은 DFS와 Branch and Bound를 사용한다.

6. 가지치기
   - 남은 조각의 총 길이가 남은 공간 총합보다 크면 중단한다.
   - 가장 긴 남은 조각이 어떤 원자재에도 들어가지 않으면 중단한다.
   - 남은 공간 구성이 같은 상태는 메모이제이션으로 재탐색하지 않는다.
   - 같은 남은 길이를 가진 원자재에는 같은 조각을 중복 시도하지 않는다.

7. 동률 해 정렬
   - 원자재 개수가 같다면 총 잔여 길이는 동일하므로 작업성이 좋은 배치를 선택한다.
   - 패턴 종류가 적은 결과를 우선한다.
   - 재사용 가능한 잔재 기준이 있다면 재사용 가능한 잔재를 최대화한다.
   - 시각적으로 읽기 쉽도록 각 원자재 안의 조각은 길이 내림차순으로 정렬한다.

## 현재 알고리즘의 한계

현재 MVP 알고리즘은 원자재 안에 절단 조각을 배치하는 문제에 집중한다.

- Best Fit Decreasing으로 빠른 초기 해를 만들고, 가능한 경우 DFS와 Branch and Bound로 최소 원자재 개수를 찾는다.
- 같은 원자재 개수 안에서는 패턴 종류를 줄이는 방향을 일부 고려하지만, 실제 작업 순서를 점수화하지 않는다.
- 원자재별 결과는 "원자재 1번을 어떻게 자를지" 중심이라, 작업자가 줄자를 같은 길이에 고정한 채 여러 원자재를 연속 절단하는 흐름은 충분히 반영되지 않는다.
- 따라서 "잔재는 조금 더 생기더라도 작업 시간이 줄어드는 배치"를 현재 알고리즘은 적극적으로 선택하지 못한다.

현장 작업에서는 잔재 최소화만큼 다음 비용도 중요하다.

- 길이 변경 비용: 줄자, 스토퍼, 장비 세팅을 다른 길이로 바꾸는 시간
- 원자재 변경 비용: 자르던 원자재를 내려놓고 다른 원자재를 가져오거나 헷갈릴 가능성
- 작업 순서 혼란 비용: 원자재별 지시와 길이별 반복 지시가 섞이면서 발생하는 실수 가능성

## 현장 작업성 최적화 개선 계획

### 1. 최적화 모드 추가

입력 옵션에 `최적화 우선순위`를 추가한다.

- 최소 잔재 우선
  - 현재 방식에 가장 가깝다.
  - 원자재 개수, 총 잔여 길이, 사용률을 가장 중요하게 본다.
  - 동률일 때 길이 변경 횟수와 원자재 변경 횟수를 줄인다.

- 최소 길이 변경 우선
  - 같은 길이를 최대한 연속해서 자르는 것을 가장 중요하게 본다.
  - 예: 1200mm를 필요한 만큼 모두 자른 뒤 900mm로 넘어간다.
  - 총 잔여 길이가 약간 늘어날 수 있지만 작업 속도와 반복성이 좋아진다.

- 최소 원자재 변경 우선
  - 한 원자재를 잡으면 가능한 한 해당 원자재에서 처리할 절단을 몰아서 진행한다.
  - 원자재 이동과 혼동을 줄이는 작업장에 적합하다.
  - 같은 길이 반복보다 원자재 단위 작업 완료를 더 중요하게 본다.

- 균형 우선
  - 잔재, 길이 변경, 원자재 변경을 모두 점수화한다.
  - 기본 추천 모드로 사용할 수 있다.
  - 작업 효율과 자재 효율 사이의 타협점을 찾는다.

### 2. 결과 구조 분리

현재 결과는 원자재별 절단 배치 중심이다. 개선 후에는 두 가지 결과를 동시에 제공한다.

- 원자재 배치표
  - 각 원자재에 어떤 길이들이 들어가는지 보여준다.
  - 잔재 계산과 자재 사용률 확인에 필요하다.

- 작업 순서표
  - 작업자가 실제로 어떤 순서로 자를지 보여준다.
  - 예: `1200mm: 원자재 1번 2개, 원자재 3번 1개 -> 900mm: 원자재 2번 4개`
  - 인쇄 보고서에서는 이 작업 순서표를 원자재 배치표보다 먼저 보여준다.

### 3. 작업성 점수 계산

각 후보 절단 계획에 대해 다음 점수를 계산한다.

```text
materialScore = totalRemainder
barScore = barCount
lengthChangeScore = numberOfLengthChanges
stockChangeScore = numberOfStockChanges
patternScore = numberOfDistinctPatterns
```

최적화 모드별로 비교 순서를 다르게 둔다.

```text
최소 잔재 우선:
  barCount -> totalRemainder -> lengthChangeScore -> stockChangeScore -> patternScore

최소 길이 변경 우선:
  lengthChangeScore -> barCount -> totalRemainder -> stockChangeScore -> patternScore

최소 원자재 변경 우선:
  stockChangeScore -> barCount -> totalRemainder -> lengthChangeScore -> patternScore

균형 우선:
  weightedTotal =
    totalRemainder * materialWeight +
    lengthChangeScore * lengthChangeWeight +
    stockChangeScore * stockChangeWeight +
    patternScore * patternWeight
```

주의할 점은 `totalRemainder`와 `lengthChangeScore`의 단위가 다르다는 것이다. 균형 우선에서는 사용자가 이해하기 쉬운 기본 가중치를 제공하고, 고급 설정에서만 조정할 수 있게 한다.

### 4. 후보 해 생성 방식 개선

현재는 하나의 휴리스틱과 정확 탐색 중심으로 후보를 만든다. 개선 후에는 여러 성격의 후보를 만든 뒤 선택 모드에 따라 가장 좋은 후보를 고른다.

- 잔재 최소 후보
  - Best Fit Decreasing 기반
  - 현재 MVP의 기본 후보

- 길이 반복 후보
  - 절단 길이별 그룹을 먼저 만들고, 같은 길이를 여러 원자재에 순차 배치한다.
  - 작업 순서표가 단순해지는 대신 일부 잔재가 늘어날 수 있다.

- 원자재 유지 후보
  - 한 원자재 안에서 가능한 절단을 먼저 채우고 다음 원자재로 넘어간다.
  - 작업자가 원자재를 자주 바꾸지 않아도 된다.

- 패턴 반복 후보
  - 같은 절단 패턴을 최대한 많이 반복하도록 만든다.
  - 대량 절단 작업에서 실수를 줄이는 데 유리하다.

### 5. 작업 순서 생성 알고리즘

원자재 배치가 정해진 뒤, 별도의 작업 순서 생성 단계를 둔다.

1. 원자재별 절단 조각을 `길이 -> 원자재 -> 수량` 형태로 재그룹화한다.
2. 최적화 모드에 따라 그룹 정렬 기준을 정한다.
3. 같은 길이는 가능한 한 연속된 작업 블록으로 묶는다.
4. 각 작업 블록 안에서는 원자재 번호 순서 또는 잔여 길이가 적은 순서로 정렬한다.
5. 인쇄 보고서에는 작업 블록마다 체크박스를 제공한다.

예시:

```text
1. 1200mm 절단
   - 원자재 1번: 2개
   - 원자재 3번: 1개

2. 900mm 절단
   - 원자재 2번: 4개
   - 원자재 3번: 1개
```

### 6. UI 개선 계획

- 입력 영역
  - `최적화 우선순위` 선택 컨트롤 추가
  - 기본값은 `최소 잔재 우선`

- 결과 요약
  - 잔여 길이뿐 아니라 `길이 변경 횟수`, `원자재 변경 횟수`, `반복 패턴 수` 표시
  - 선택한 모드에 따라 어떤 기준이 우선 적용됐는지 표시

- 결과 탭
  - `작업 순서` 탭: 실제 작업자가 따라갈 순서
  - `원자재 배치` 탭: 자재별 절단 구성과 잔재
  - `패턴 요약` 탭: 반복 가능한 절단 패턴

- 인쇄 보고서
  - 1순위로 작업 순서표 출력
  - 2순위로 원자재별 배치표 출력
  - 길이별 작업 블록마다 체크박스 제공

### 7. 구현 단계

1. 타입 확장
   - `optimizationMode` 추가
   - `workSteps`, `lengthChangeCount`, `stockChangeCount`, `patternCount` 추가

2. 점수 계산기 추가
   - 후보 절단 계획을 평가하는 `scorePlan` 모듈 작성
   - 모드별 비교 함수 작성

3. 후보 생성기 추가
   - 기존 Best Fit Decreasing 유지
   - 길이 반복 우선 후보 생성
   - 원자재 유지 우선 후보 생성
   - 패턴 반복 우선 후보 생성

4. 작업 순서 생성기 추가
   - 절단 배치를 길이별 작업 블록으로 변환
   - 원자재 번호와 수량을 함께 표시

5. UI 반영
   - 최적화 모드 선택 추가
   - 결과 요약 지표 추가
   - 작업 순서 탭 추가

6. 인쇄 보고서 반영
   - 작업 순서표를 보고서 상단으로 이동
   - 길이별 체크박스 추가

7. 테스트 추가
   - 같은 잔재 조건에서 길이 변경이 적은 결과를 선택하는지 검증
   - 같은 잔재 조건에서 원자재 변경이 적은 결과를 선택하는지 검증
   - 선택 모드별 결과가 달라질 수 있음을 회귀 테스트로 고정

### 8. 추천 기본 정책

실제 작업자 사용성을 고려하면 기본값은 `균형 우선`을 추천한다.

다만 사용자가 프로젝트 목적을 명확히 선택할 수 있도록 다음 프리셋을 제공한다.

- 자재 비용이 가장 중요할 때: 최소 잔재 우선
- 줄자/스토퍼 세팅 변경 시간이 오래 걸릴 때: 최소 길이 변경 우선
- 원자재 이동과 혼동을 줄이고 싶을 때: 최소 원자재 변경 우선
- 일반적인 현장 작업: 균형 우선


## MVP 완료 기준

- 사용자가 원자재 길이와 절단 항목을 입력할 수 있다.
- 최적 원자재 개수와 원자재별 절단 배치를 확인할 수 있다.
- 총 잔여 길이와 사용률을 확인할 수 있다.
- 같은 절단 패턴이 묶여 표시된다.
- 모바일과 데스크톱에서 결과를 읽을 수 있다.
- Vercel Production URL에서 정상 동작한다.


# 프로젝트 확장 계획
- 프로젝트를 잘라야 하는 길이와 수량을 입력 받았을 때 "최적의 방법"을 계산해주는 것을 넘어, 창문의 가로(w), 세로(h), 종류, 옵션, 슬라이딩 넓이 등을 입력받아 직접 잘라야 하는 길이와 수량을 계산한 후, "최적의 방법"까지 제안해주는 것으로 확장하려고 한다.

- 현재 현장에서의 작업 순서는 다음과 같다.
    1. 제작하려는 창문의 가로 길이, 세로 길이, 수량, 종류, 슬라이딩 길이, 방충망 추가 여부를 확인한다.
        - 창문의 종류로는 다음과 같이 5가지가 있다.
            - 정양쪽문: 가로 길이가 1:1 비율인 창문 두짝이 열리는 창문
            - 일반삼중문: 가로 길이가 1:2:1 비율인 창문 세짝이 열리는 창문
            - 정삼중문: 가로 길이가 1:1:1 비율인 창문 세짝이 열리는 창문
            - 4중문: 가로 길이가 1:1:1:1 비율인 창문 네짝이 가운데를 기준으로 서로 열리는 창문.
            - 언발란스문: 창문 두짝이 열리는 창문인데 길이가 긴 쪽은 고정이고 짧은 쪽 창문이 열리는 창문으로 길이는 그때그때 다름
        - 슬라이딩 길이는 다음과 같이 2가지가 있다.
            - 92mm
            - 115mm
    2. 필요한 길이와 수량을 계산할 때는 창문의 "프레임"과 창문에 사용될 "창틀"을 각각 계산한다. 또한, 방충망을 추가하는 경우에는 방충망에 필요한 길이와 수량도 추가로 계산해야 한다.
    3. 프레임에 필요한 길이는 입력받은 창문 가로 길이와 세로 길이 그대로이며, 수량은 상,하,좌,우에 필요하기 때문에 '창문 수량 x 2'를 적용하면 되고 이는 창문의 종류와 슬라이딩 길이와 상관없이 모두 동일하다.
    4. 하지만 창틀의 경우에는 창문의 종류와 슬라이딩 길이에 따라 잘라야 하는 길이와 수량을 계산하는 공식이 다르기 때문에, 창문의 종류와 슬라이딩 길이를 확인한 후 알맞은 공식에 따라 필요한 길이와 수량을 계산한다.
    5. 창문의 가로와 세로는 다시 한 번 각각 '상바', '로라바'와 '고리', '손잡이'로 구분되며 가로는 창문의 종류와 상관없이 동일한 수량으로 각각 나눠지고 세로는 창문의 종류에 따라 나눠지는 수량이 다르다.

- 현장에서의 작업 순서를 고려하여 서비스를 설계하는데 현재 경우의 수가 많으므로 우선 '정양쪽문' 계산만 개발한 후, 현장 테스트 진행 후에 다른 종류들로 확대한다.

- '정양쪽문'의 창틀 길이 계산 공식은 아래와 같다
    - 슬라이딩 길이가 92mm인 경우
        - w(가로): 입력값 기준으로 140mm 빼고 나누기 2
		    - 방충망을 추가하는 경우 방충망 w 길이: 계산한 w치수에서 60mm를 더함
	    - h(높이): 입력값 기준으로 63mm 빼기
		    - 방충망을 추가하는 경우 방충망 h 길이: 입력값에서 53mm를 뺌(계산한 h치수에서 10mm 더함이랑 같음)
    
    - 슬라이딩 길이가 115mm인 경우
        - w(가로): 입력값 기준으로 190mm 빼고 나누기 2
		    - 방충망을 추가하는 경우 방충망 w 길이: 계산한 w치수에서 100mm를 더함
	    - h(높이): 입력값 기준으로 67mm 빼기
		    - 방충망을 추가하는 경우 방충망 h 길이: 프레임에서 57mm를 뺌(h치수에서 10mm 더함이랑 같음)
- '정양쪽문'의 창틀 수량 계산 공식은 아래와 같다
    - w(가로): '입력 창문 수량 * 4'
        - 방충망을 추가하는 경우 방충망 가로 수량: '입력 창문 수량 * 2'
    - h(세로): '입력 창문 수량 * 4'
        - 방충망을 추가하는 경우 방충망 세로 수량: '입력 창문 수량 * 2'
- '정양쪽문'의 '고리', '손잡이'는 '상바', '로라바'와 마찬가지로 동일한 수량으로 나눠진다.


# 확장 개발 계획

## 확장 목표

현재 서비스는 사용자가 잘라야 하는 길이와 수량을 직접 입력하면 최적 절단 계획을 계산한다.

확장 버전에서는 사용자가 창문 제작 정보를 입력하면 서비스가 먼저 필요한 절단 목록을 자동 계산하고, 그 결과를 기존 최적 절단 엔진에 연결한다.

확장 후 흐름:

```text
창문 정보 입력
-> 프레임/창틀/방충망 절단 목록 자동 계산
-> 사용자가 산출 목록 확인
-> 프레임/창틀/방충망별로 각각 최적 절단 계산
-> 부품군별 작업 순서/원자재 배치/인쇄 보고서 출력
```

## 1차 확장 범위

경우의 수가 많으므로 1차 확장에서는 `정양쪽문`만 지원한다.

- 지원 창문 종류: 정양쪽문
- 지원 슬라이딩 길이: 92mm, 115mm
- 지원 옵션: 방충망 추가 여부
- 프레임, 창틀, 방충망은 서로 다른 원자재를 사용하므로 각각 별도 원자재 길이를 입력받는다.
- 기존 직접 절단 목록 입력 기능은 유지한다.
- 새 기능은 `창문 정보로 계산` 모드로 추가한다.

1차 확장에서 제외할 항목:

- 일반삼중문
- 정삼중문
- 4중문
- 언발란스문
- 여러 창문 종류를 한 번에 섞어서 계산하는 기능
- 견적, 단가, 재고 관리

## 사용자 흐름

### 직접 입력 모드

기존 방식 그대로 유지한다.

1. 원자재 길이 입력
2. 절단 길이와 수량 직접 입력
3. 최적화 우선순위 선택
   - 기본값: 최소 잔재 우선
4. 최적 절단 계산

### 창문 정보 입력 모드

새로 추가할 흐름이다.

1. 부품군별 원자재 길이 입력
   - 프레임 원자재 길이: 기본값 6300mm
   - 창틀 원자재 길이: 기본값 6300mm
   - 방충망 원자재 길이: 기본값 6300mm
2. 창문 종류 선택
   - 1차 버전에서는 `정양쪽문`만 선택 가능
3. 슬라이딩 길이 선택
   - 92mm
   - 115mm
4. 창문 가로 길이 입력
5. 창문 세로 길이 입력
6. 창문 수량 입력
7. 방충망 추가 여부 선택
8. 절단 목록 자동 산출
9. 산출된 절단 목록 확인
10. 프레임/창틀/방충망별 최적 절단 계산

## 정양쪽문 계산 규칙

### 공통 프레임 계산

프레임은 창문 종류와 슬라이딩 길이에 관계없이 동일하게 계산한다.

- 프레임 가로 길이: 입력 w
- 프레임 가로 수량: 창문 수량 * 2
- 프레임 세로 길이: 입력 h
- 프레임 세로 수량: 창문 수량 * 2

프레임 수량의 의미:

- 창문 1개는 상, 하, 좌, 우 프레임으로 구성된다.
- 상/하는 가로 프레임이므로 창문 1개당 가로 2개가 필요하다.
- 좌/우는 세로 프레임이므로 창문 1개당 세로 2개가 필요하다.
- 따라서 가로와 세로 모두 각각 `창문 수량 * 2`를 적용한다.

길이 계산 결과가 소수점이면 반올림해서 자연수 mm로 만든다.

### 슬라이딩 92mm 창틀 계산

- 창틀 가로 길이: `(w - 140) / 2`
- 창틀 가로 수량: 창문 수량 * 4
- 창틀 세로 길이: `h - 63`
- 창틀 세로 수량: 창문 수량 * 4

방충망 추가 시:

- 방충망 가로 길이: `((w - 140) / 2) + 60`
- 방충망 가로 수량: 창문 수량 * 2
- 방충망 세로 길이: `h - 53`
- 방충망 세로 수량: 창문 수량 * 2

### 슬라이딩 115mm 창틀 계산

- 창틀 가로 길이: `(w - 190) / 2`
- 창틀 가로 수량: 창문 수량 * 4
- 창틀 세로 길이: `h - 67`
- 창틀 세로 수량: 창문 수량 * 4

방충망 추가 시:

- 방충망 가로 길이: `((w - 190) / 2) + 100`
- 방충망 가로 수량: 창문 수량 * 2
- 방충망 세로 길이: `h - 57`
- 방충망 세로 수량: 창문 수량 * 2

## 데이터 모델 확장

### 창문 입력 모델

```ts
type WindowType = "double-even";

type SlidingSize = 92 | 115;

type WindowSpecInput = {
  id: string;
  type: WindowType;
  slidingSize: SlidingSize;
  width: number;
  height: number;
  quantity: number;
  hasScreen: boolean;
};
```

### 산출 절단 항목 모델

기존 `CutItem`에 산출 근거를 추적할 수 있는 메타데이터를 추가한다.

```ts
type CutItemCategory = "frame" | "sash" | "screen";

type GeneratedCutItem = {
  id: string;
  label: string;
  length: number;
  quantity: number;
  category: CutItemCategory;
  sourceWindowId: string;
  formulaNote: string;
};
```

### 부품군별 원자재 모델

프레임, 창틀, 방충망은 서로 다른 원자재를 사용하며 원자재 길이도 다르다.

```ts
type MaterialGroup = "frame" | "sash" | "screen";

type MaterialStockInput = {
  frameStockLength: number;
  sashStockLength: number;
  screenStockLength?: number;
};
```

방충망을 추가하지 않는 경우 `screenStockLength`는 입력받지 않는다.

기본값은 모두 6300mm로 둔다.

### 부품군별 최적화 결과 모델

자동 산출된 절단 항목은 부품군별로 나눠 각각 최적화 계산을 수행한다.

```ts
type GroupedCutPlan = {
  group: MaterialGroup;
  stockLength: number;
  items: GeneratedCutItem[];
  plan: CutPlan;
};
```

### 기존 최적화 엔진과 연결

`GeneratedCutItem[]`은 부품군별로 나눈 뒤 기존 `CutItem[]`으로 변환해 현재 solver에 전달한다.

```text
WindowSpecInput
-> GeneratedCutItem[]
-> group by frame/sash/screen
-> frame CutItem[] + frame stock length -> solveCutPlan()
-> sash CutItem[] + sash stock length -> solveCutPlan()
-> screen CutItem[] + screen stock length -> solveCutPlan()
```

같은 길이가 서로 다른 부품군에서 나와도 합산하지 않는다. 프레임, 창틀, 방충망은 원자재가 다르므로 별도의 절단 계획으로 유지한다.

같은 부품군 안에서 같은 길이가 여러 번 나오면 합산해서 표시하고 계산한다. 보고서에는 부품군과 산출 근거를 유지한다.

## 코드 구조 계획

예상 추가 구조:

```text
src/
  lib/
    windowRules/
      types.ts
      doubleEven.ts
      index.ts
      windowRules.test.ts
    groupedSolver/
      types.ts
      index.ts
      groupedSolver.test.ts
  components/
    WindowInputForm.tsx
    GeneratedCutList.tsx
    GroupedCutPlanSummary.tsx
    InputModeTabs.tsx
```

### `windowRules/types.ts`

- 창문 종류 타입 정의
- 슬라이딩 타입 정의
- 창문 입력 타입 정의
- 자동 산출 절단 항목 타입 정의

### `windowRules/doubleEven.ts`

- 정양쪽문 계산 공식 구현
- 92mm/115mm 슬라이딩별 계산 분기
- 방충망 포함/미포함 계산
- 모든 계산 결과는 반올림해 자연수 mm로 변환

### `windowRules/index.ts`

- 창문 종류별 계산 함수 라우팅
- 1차 버전에서는 정양쪽문만 연결
- 추후 다른 창문 종류 추가 시 이 파일을 확장

### `WindowInputForm.tsx`

- 창문 종류
- 슬라이딩 길이
- 가로/세로 길이
- 수량
- 방충망 추가 여부 입력

### `GeneratedCutList.tsx`

- 자동 산출된 절단 목록 표시
- 프레임/창틀/방충망 그룹별 표시
- 사용자가 최적 절단 계산 전에 산출 내역을 검토할 수 있게 함

### `GroupedCutPlanSummary.tsx`

- 프레임/창틀/방충망별 최적화 결과를 나눠 표시
- 각 부품군의 원자재 개수, 잔여 길이, 작업 순서를 별도로 표시
- 방충망이 없는 경우 방충망 섹션은 표시하지 않음

### `InputModeTabs.tsx`

- `직접 입력`
- `창문 정보로 계산`

두 입력 흐름을 전환한다.

## UI 개발 계획

### 1단계: 입력 모드 분리

현재 입력 패널 상단에 모드 선택을 추가한다.

- 직접 입력
- 창문 정보로 계산

직접 입력 모드는 기존 UI를 유지한다.

### 2단계: 정양쪽문 입력 폼 추가

창문 정보 입력 모드에서 다음 입력을 받는다.

- 프레임 원자재 길이
  - 기본값 6300mm
- 창틀 원자재 길이
  - 기본값 6300mm
- 방충망 원자재 길이
  - 기본값 6300mm
  - 방충망 추가 시에만 입력
- 창문 종류: 정양쪽문
- 슬라이딩 길이: 92mm / 115mm
- 가로 길이
- 세로 길이
- 수량
- 방충망 추가 여부

### 3단계: 자동 산출 목록 표시

계산 버튼을 누르기 전 또는 입력 변경 시 자동으로 산출 목록을 보여준다.

표시 예시:

```text
프레임
- 가로 1800mm x 2개
- 세로 1200mm x 2개

창틀
- 가로 830mm x 4개
- 세로 1137mm x 4개

방충망
- 가로 890mm x 2개
- 세로 1147mm x 2개
```

### 4단계: 기존 최적 절단 계산과 연결

자동 산출 목록을 부품군별 기존 절단 항목 목록으로 변환한다.

- 사용자는 자동 산출 목록 확인 후 `최적 절단 계산`을 누른다.
- 최적화 우선순위 기본값은 `최소 잔재 우선`으로 둔다.
- 절단 손실값 기본값은 프레임/창틀/방충망 모두 0mm로 둔다.
- 원자재 길이는 프레임/창틀/방충망별 입력값을 사용한다.
- 결과 화면은 기존 작업 순서/원자재 배치/인쇄 보고서를 부품군별로 재사용한다.
- 프레임, 창틀, 방충망은 같은 길이가 있어도 서로 합산하지 않고 각각 별도 계산한다.

### 5단계: 인쇄 보고서 개선

창문 정보 입력 모드에서 만든 결과라면 인쇄 보고서 상단에 창문 입력 정보를 추가한다.

- 창문 종류
- 슬라이딩 길이
- 입력 w/h
- 창문 수량
- 방충망 여부

그리고 자동 산출된 절단 목록을 별도 섹션으로 표시한다.

보고서 구성:

1. 창문 입력 정보
2. 자동 산출 절단 목록
3. 프레임 절단 계획
4. 창틀 절단 계획
5. 방충망 절단 계획
   - 방충망이 있는 경우에만 표시

## 검증 계획

### 단위 테스트

`windowRules/doubleEven.ts`에 대해 공식별 테스트를 작성한다.

테스트 케이스:

- 정양쪽문 92mm, 방충망 없음
- 정양쪽문 92mm, 방충망 있음
- 정양쪽문 115mm, 방충망 없음
- 정양쪽문 115mm, 방충망 있음
- 수량이 2 이상일 때 모든 수량 배수가 맞는지 검증
- 소수점 길이가 반올림되는지 검증
- 계산 결과가 0 이하가 되는 비정상 입력을 오류 처리하는지 검증

### 통합 테스트

- 창문 정보 입력값을 넣으면 자동 절단 목록이 생성되는지 확인
- 자동 절단 목록이 프레임/창틀/방충망별 최적 절단 계산에 따로 전달되는지 확인
- 인쇄 보고서에 창문 정보와 산출 목록이 표시되는지 확인
- 같은 길이가 서로 다른 부품군에 있어도 합산되지 않는지 확인

### 수동 테스트

현장 테스트용 대표 입력값을 정해 배포 환경에서 확인한다.

- 92mm 정양쪽문 1세트
- 92mm 정양쪽문 여러 세트
- 115mm 정양쪽문 1세트
- 방충망 포함 케이스
- 모바일 화면에서 입력과 결과가 읽기 쉬운지 확인

## 개발 순서

### 1단계: 도메인 계산 로직 분리

- `src/lib/windowRules` 폴더 추가
- 정양쪽문 타입과 공식 구현
- 단위 테스트 작성

### 2단계: 자동 산출 결과를 기존 절단 항목으로 변환

- `GeneratedCutItem`을 기존 `CutItem`으로 변환하는 함수 작성
- 부품군별로 `GeneratedCutItem`을 그룹화하는 함수 작성
- 같은 부품군 안의 같은 길이는 합산하되, 서로 다른 부품군은 절대 합산하지 않음
- 프레임/창틀/방충망 각각 기존 solver와 연결 테스트

### 3단계: 입력 UI 확장

- 입력 모드 탭 추가
- 정양쪽문 입력 폼 추가
- 산출 절단 목록 미리보기 추가

### 4단계: 결과/보고서 확장

- 결과 화면에 입력 모드와 산출 기준 표시
- 결과 화면에 프레임/창틀/방충망별 절단 계획 표시
- 인쇄 보고서에 창문 정보와 자동 산출 목록 추가
- 인쇄 보고서에 부품군별 절단 계획 표시

### 5단계: 배포 전 검증

- `npm run test`
- `npm run build`
- 배포 환경에서 정양쪽문 대표 케이스 테스트

### 6단계: 현장 테스트 후 확장

정양쪽문 현장 테스트가 끝난 뒤 다음 창문 종류를 하나씩 추가한다.

추천 순서:

1. 정삼중문
2. 일반삼중문
3. 4중문
4. 언발란스문

각 창문 종류는 반드시 다음 순서로 추가한다.

```text
공식 문서화
-> 계산 함수 구현
-> 단위 테스트
-> UI 선택지 추가
-> 현장 테스트
```

## 확정된 정책

1. 프레임 수량은 가로와 세로 모두 각각 `창문 수량 * 2`를 적용한다.
   - 창문 1개 기준 상/하가 가로 2개, 좌/우가 세로 2개이기 때문이다.
2. 프레임, 창틀, 방충망은 원자재가 모두 다르고 원자재 길이도 다르므로 각각 따로 최적화 계산한다.
3. 같은 길이라도 프레임/창틀/방충망처럼 출처가 다르면 합산하지 않는다.
4. 계산 결과가 소수점이면 반올림해서 자연수 mm로 만든다.
5. 방충망은 별도 원자재를 사용한다.
6. 프레임, 창틀, 방충망의 기본 원자재 길이는 모두 6300mm로 둔다.
7. 절단 손실값은 기본적으로 모두 0mm로 둔다.
8. 같은 부품군 안에서 같은 길이가 중복되면 합산 표시하고 합산 계산한다.
9. 창문 정보 입력 모드 1차 버전은 한 규격만 입력하는 것으로 개발한다.
10. 최적화 우선순위 기본값은 `최소 잔재 우선`으로 둔다.

## 추가 확인이 필요한 사항

현재 1차 개발에 필요한 정책은 모두 확정되었다.

현장 테스트 후 추가 확인이 필요한 항목:

1. 부품군별 절단 손실값을 실제 장비 기준으로 따로 입력해야 하는지
2. 여러 창문 규격을 한 번에 입력하는 기능이 필요한지
3. 정양쪽문 외 다음으로 우선 개발할 창문 종류
